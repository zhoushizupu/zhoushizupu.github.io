# 部署方案

## 1. 部署概述

本项目采用静态网站部署方案，可通过多种免费平台托管，重点推荐使用 Gitee Pages 进行部署，确保完全免费且符合项目需求。

## 2. 部署平台

### 2.1 GitHub Pages 部署

1. **创建 GitHub 仓库**
   - 登录 GitHub，创建新仓库
   - 克隆仓库到本地

2. **构建项目**
   ```bash
   npm run build
   ```

3. **部署到 GitHub Pages**
   - 安装 `gh-pages` 包
   ```bash
   npm install gh-pages --save-dev
   ```
   
   - 在 `package.json` 中添加脚本
   ```json
   "scripts": {
     "deploy": "gh-pages -d dist"
   }
   ```
   
   - 执行部署
   ```bash
   npm run deploy
   ```

4. **配置 GitHub Pages**
   - 进入仓库设置 → Pages
   - 选择 `gh-pages` 分支作为发布源
   - 保存设置，等待部署完成

### 2.2 Vercel 部署

1. **连接 GitHub 仓库**
   - 登录 Vercel
   - 点击 "New Project"
   - 选择你的 GitHub 仓库

2. **配置部署**
   - 选择框架：React
   - 构建命令：`npm run build`
   - 输出目录：`dist`

3. **部署**
   - 点击 "Deploy"
   - 等待部署完成
   - 获取部署 URL

### 2.3 Netlify 部署

1. **连接 GitHub 仓库**
   - 登录 Netlify
   - 点击 "Add new site" → "Import an existing project"
   - 选择你的 GitHub 仓库

2. **配置部署**
   - 构建命令：`npm run build`
   - 发布目录：`dist`

3. **部署**
   - 点击 "Deploy site"
   - 等待部署完成
   - 获取部署 URL

### 2.4 Gitee Pages 部署（推荐）

1. **创建 Gitee 仓库**
   - 登录 Gitee，创建新仓库
   - 克隆仓库到本地
   ```bash
   git clone https://gitee.com/your-username/family-tree.git
   cd family-tree
   ```

2. **构建项目**
   ```bash
   npm run build
   ```

3. **部署到 Gitee Pages**
   - 推送构建产物到仓库
   ```bash
   git add .
   git commit -m "Build and deploy"
   git push origin main
   ```

4. **配置 Gitee Pages**
   - 进入仓库设置 → Pages
   - 选择分支：`main`
   - 选择目录：`dist`
   - 点击 "启动"
   - 等待部署完成，获取访问 URL

5. **媒体文件处理**
   - 将头像等媒体文件放置在 `public/media/` 目录
   - 构建时会自动复制到 `dist/media/` 目录
   - 确保在数据结构中正确引用媒体文件路径

6. **免费托管说明**
   - Gitee Pages 提供免费的静态网站托管服务
   - 支持自定义域名（需在仓库设置中配置）
   - 无流量限制，适合中小型族谱网站
   - 媒体文件大小限制：单个文件不超过 100MB，总存储不超过 1GB
   - 国内访问速度快，适合中国用户

## 3. 部署前准备

### 3.1 项目构建

1. **执行构建命令**
   ```bash
   npm run build
   ```

2. **验证构建结果**
   - 检查 `dist` 目录是否生成
   - 确保所有静态文件和媒体文件正确复制
   - 验证 `family.json` 数据文件存在

### 3.2 媒体文件准备

1. **创建媒体目录**
   ```bash
   mkdir -p public/media
   ```

2. **添加头像文件**
   - 将家族成员的头像图片放入 `public/media/` 目录
   - 文件名建议使用 `I{id}-avatar.jpg` 格式，如 `I1-avatar.jpg`

3. **更新数据结构**
   - 在 `family.json` 文件中为每个成员添加 `media` 字段
   - 正确引用头像文件路径

## 4. 部署后验证

### 4.1 功能验证

1. **访问网站**
   - 打开部署后的 URL
   - 验证网站是否正常加载

2. **测试核心功能**
   - 家族树显示是否正常
   - 点击头像是否能查看个人详情
   - 导航和搜索功能是否正常
   - 密码保护功能是否生效

3. **测试媒体文件**
   - 验证头像是否正确显示
   - 检查其他媒体文件是否可访问

### 4.2 性能验证

1. **加载速度测试**
   - 使用浏览器开发者工具测试页面加载时间
   - 确保页面加载时间 < 3秒

2. **响应式测试**
   - 在不同设备上测试网站显示
   - 确保在手机、平板和桌面端都能正常显示

3. **兼容性测试**
   - 在不同浏览器中测试网站
   - 确保在 Chrome、Firefox、Safari、Edge 中都能正常运行

## 5. 维护与更新

### 5.1 数据更新

1. **修改数据**
   - 编辑 `public/data/family.json` 文件
   - 添加新成员或修改现有成员信息

2. **重新构建**
   ```bash
   npm run build
   ```

3. **重新部署**
   ```bash
   git add .
   git commit -m "Update family data"
   git push origin main
   ```

4. **验证更新**
   - 等待 Gitee Pages 重新部署
   - 访问网站验证更新是否生效

### 5.2 功能更新

1. **修改代码**
   - 更新前端组件或样式
   - 添加新功能

2. **重新构建和部署**
   - 按照上述步骤重新构建和部署

3. **测试新功能**
   - 确保新功能正常工作
   - 验证现有功能不受影响

## 6. 故障排查

### 6.1 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 网站无法访问 | 部署配置错误 | 检查 Gitee Pages 配置，确保分支和目录设置正确 |
| 头像不显示 | 路径错误 | 检查头像文件路径是否正确，确保文件存在于 `public/media/` 目录 |
| 家族树不显示 | 数据格式错误 | 检查 `family.json` 文件格式，确保符合数据结构要求 |
| 页面加载缓慢 | 数据量过大 | 优化数据结构，实现分层加载 |

### 6.2 调试技巧

1. **浏览器控制台**
   - 使用浏览器开发者工具查看控制台错误
   - 检查网络请求是否正常

2. **构建日志**
   - 检查构建过程中的错误信息
   - 确保所有文件都正确构建

3. **Gitee Pages 状态**
   - 查看 Gitee Pages 部署状态
   - 确认部署是否成功完成

---

**文档更新时间：** 2026-04-18
**作者：** 族谱项目团队