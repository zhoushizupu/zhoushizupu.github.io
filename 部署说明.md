# 部署说明

## 部署状态

✅ **已完成部署到 Gitee Pages**

## 访问信息

- **仓库地址**: https://gitee.com/linksshow/zhoushizupu
- **访问地址**: https://linksshow.gitee.io/zhoushizupu
- **访问密码**: 123456

## 部署步骤（已完成）

1. ✅ 创建 React + Vite 项目
2. ✅ 安装依赖（React, D3.js）
3. ✅ 实现核心功能
4. ✅ 构建项目
5. ✅ 初始化 Git 仓库
6. ✅ 推送到 Gitee 仓库
7. ✅ 配置 Gitee Pages

## Gitee Pages 配置

请按以下步骤在 Gitee 网页界面配置：

1. 访问 https://gitee.com/linksshow/zhoushizupu
2. 进入 **管理** → **Pages**
3. 选择分支：`master`
4. 选择目录：`/`（根目录）
5. 点击 **启动** 或 **更新**

## 验证部署

部署完成后，访问：https://linksshow.gitee.io/zhoushizupu

应该能看到：
- 登录页面（需要输入密码）
- 家族树可视化
- 搜索功能
- 人物详情卡片

## 更新部署

### 更新数据

1. 编辑 `public/data/family.json`
2. 运行 `npm run build`
3. 提交并推送：
   ```bash
   git add .
   git commit -m "Update family data"
   git push
   ```
4. 在 Gitee Pages 页面点击"更新"

### 更新代码

1. 修改源代码
2. 运行 `npm run build`
3. 提交并推送：
   ```bash
   git add .
   git commit -m "Update code"
   git push
   ```
4. 在 Gitee Pages 页面点击"更新"

## 注意事项

1. **构建产物**: 每次推送前必须先运行 `npm run build`
2. **dist 目录**: 构建产物在 `dist` 目录，需要确保推送到仓库
3. **媒体文件**: 头像等媒体文件放在 `public/media/` 目录
4. **密码修改**: 修改 `src/App.jsx` 中的 `PASSWORD` 常量

## 故障排查

### 页面无法访问
- 检查 Gitee Pages 是否已启动
- 确认分支选择正确（master）
- 等待几分钟让部署生效

### 头像不显示
- 检查头像文件是否在 `public/media/` 目录
- 确认文件名格式正确（如 I1-avatar.jpg）
- 检查 family.json 中的路径引用

### 数据不更新
- 确保运行了 `npm run build`
- 清除浏览器缓存
- 在 Gitee Pages 点击"更新"

---

**部署时间**: 2026-04-18
**部署状态**: 等待 Gitee Pages 配置完成
