# 族谱静态网页开发安排和步骤

## 1. 项目概述

本项目旨在开发一个基于静态网页技术的族谱系统，参考 Webtrees 功能，但采用完全静态的方式实现，可通过 Gitee Pages 等免费平台托管。

### 1.1 项目目标
- 提供家谱数据的静态展示功能
- 支持家族树可视化
- 实现数据导入/导出功能
- 保持开源、免费特性
- 方便代码管理和版本控制

### 1.2 技术选型

| 类别 | 技术/工具 | 版本 | 用途 |
|------|-----------|------|------|
| 前端框架 | React | 18+ | 构建用户界面 |
| 可视化库 | D3.js | 7+ | 家族树可视化 |
| 数据格式 | JSON | - | 存储家谱数据 |
| 构建工具 | Vite | 4+ | 项目构建和开发 |
| 托管平台 | Gitee Pages | - | 免费静态托管 |

## 2. 开发步骤

### 2.1 第一阶段：项目初始化（第1天）

#### 2.1.1 环境搭建
1. **创建项目目录**
   ```bash
   mkdir family-tree
   cd family-tree
   ```

2. **初始化 React 项目**
   ```bash
   npm create vite@latest . -- --template react
   ```

3. **安装依赖**
   ```bash
   npm install
   npm install d3
   ```

4. **创建项目结构**
   - 创建 `public/data/` 目录用于存放 JSON 数据
   - 创建 `src/components/` 目录用于存放组件
   - 创建 `src/utils/` 目录用于存放工具函数

#### 2.1.2 项目配置
1. **配置 vite.config.js**
   - 设置构建选项
   - 配置开发服务器

2. **初始化 Git 仓库**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

### 2.2 第二阶段：数据层实现（第2-3天）

#### 2.2.1 数据结构设计
1. **人物数据结构**
   - 设计包含基本信息、关系、事件的数据结构
   - 定义字段类型和约束

2. **家族树数据结构**
   - 设计包含个人、家庭、根节点的数据结构
   - 确保数据结构支持家族树的构建和遍历

#### 2.2.2 GEDCOM 转换工具
1. **实现 GEDCOM 解析器**
   - 解析 GEDCOM 文件格式
   - 转换为 JSON 数据结构

2. **测试转换功能**
   - 使用示例 GEDCOM 文件测试
   - 验证转换结果的正确性

#### 2.2.3 创建示例数据
1. **生成示例 JSON 数据**
   - 创建包含多个家族成员的示例数据
   - 确保数据结构完整且符合规范

2. **验证数据结构**
   - 检查数据格式是否正确
   - 确保数据可以被前端组件正确加载

### 2.3 第三阶段：前端核心功能（第4-6天）

#### 2.3.1 家族树可视化组件
1. **实现基本树结构**
   - 使用 D3.js 创建树状布局
   - 实现节点和连接线的绘制
   - 添加头像显示功能

2. **添加交互功能**
   - 支持缩放和平移
   - 实现节点点击事件，显示个人详情
   - 添加悬停效果
   - 实现头像点击查看详情功能

3. **优化渲染性能**
   - 处理大数据量时的性能问题
   - 实现分层加载或虚拟滚动
   - 优化头像加载性能

#### 2.3.2 人物卡片组件
1. **设计卡片布局**
   - 展示个人基本信息（姓名、性别、出生日期、逝世日期、籍贯、职业、教育背景）
   - 显示成就与荣誉
   - 展示生平简介
   - 列出重要事件（出生、结婚、逝世等）
   - 显示家族关系（父母、配偶、子女）
   - 响应式设计，适配不同屏幕尺寸

2. **实现交互功能**
   - 点击节点显示详情
   - 支持关闭卡片
   - 头像显示与处理
   - 平滑的动画效果

3. **样式设计**
   - 现代化的卡片布局
   - 清晰的信息层次结构
   - 美观的头像展示方式
   - 响应式设计，支持移动端

#### 2.3.3 导航组件
1. **设计导航菜单**
   - 提供快速访问功能
   - 支持跳转到不同分支

2. **实现搜索功能**
   - 支持按姓名搜索家族成员
   - 显示搜索结果

### 2.4 第四阶段：密码保护功能（第7天）

#### 2.4.1 实现客户端密码验证
1. **创建登录界面**
   - 设计密码输入表单
   - 添加错误提示

2. **实现验证逻辑**
   - 密码验证
   - 访问控制

3. **优化用户体验**
   - 添加动画效果
   - 确保界面美观

### 2.5 第五阶段：测试与优化（第8天）

#### 2.5.1 功能测试
1. **测试核心功能**
   - 家族树显示
   - 人物卡片功能
   - 导航和搜索
   - 密码保护

2. **兼容性测试**
   - 测试不同浏览器
   - 测试不同设备

#### 2.5.2 性能优化
1. **优化数据加载**
   - 减少数据大小
   - 优化加载速度

2. **优化渲染性能**
   - 减少 DOM 操作
   - 优化动画效果

### 2.6 第六阶段：部署与上线（第9天）

#### 2.6.1 构建项目
1. **执行构建命令**
   ```bash
   npm run build
   ```

2. **验证构建结果**
   - 检查构建产物
   - 确保所有文件正确生成

#### 2.6.2 部署到 Gitee Pages
1. **创建 Gitee 仓库**
   - 登录 Gitee
   - 创建新仓库

2. **推送代码**
   ```bash
   git remote add origin https://gitee.com/your-username/family-tree.git
   git push -u origin main
   ```

3. **配置 Gitee Pages**
   - 进入仓库设置
   - 启用 Gitee Pages
   - 选择分支和目录

4. **验证部署结果**
   - 访问部署 URL
   - 测试所有功能

### 2.7 第七阶段：文档完善（第10天）

#### 2.7.1 编写 README 文档
1. **项目说明**
   - 项目概述
   - 技术栈
   - 功能特性

2. **使用指南**
   - 如何访问
   - 如何使用
   - 如何更新数据

#### 2.7.2 编写维护指南
1. **数据管理**
   - 如何添加新成员
   - 如何修改数据

2. **常见问题**
   - 问题排查
   - 解决方案

## 3. 开发注意事项

### 3.1 代码规范
- 遵循 React 最佳实践
- 使用 ESLint 和 Prettier 确保代码质量
- 保持代码风格一致

### 3.2 性能考虑
- 优化数据结构
- 减少不必要的渲染
- 合理使用缓存

### 3.3 安全性
- 密码保护实现
- 数据隐私保护
- 防止 XSS 攻击

### 3.4 可维护性
- 模块化设计
- 清晰的代码结构
- 完善的注释

## 4. 开发资源

### 4.1 开发环境
- Node.js 16+
- npm 8+
- VS Code
- Git

### 4.2 参考文档
- [React 文档](https://react.dev/)
- [D3.js 文档](https://d3js.org/)
- [Vite 文档](https://vitejs.dev/)
- [Gitee Pages 文档](https://gitee.com/help/articles/4136)

### 4.3 工具资源
- GEDCOM 解析工具
- 家族树可视化库
- 密码加密工具

## 5. 项目管理

### 5.1 版本控制
- 使用 Git 进行版本管理
- 遵循 Git 工作流
- 定期提交代码

### 5.2 任务跟踪
- 使用 GitHub Issues 或其他工具跟踪任务
- 定期更新任务状态
- 确保按时完成任务

### 5.3 沟通协作
- 定期召开开发会议
- 及时沟通问题和解决方案
- 确保团队成员了解项目进展

---

**文档更新时间：** 2026-04-18
**作者：** 族谱项目团队