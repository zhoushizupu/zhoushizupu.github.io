# 族谱静态网页开发文档

## 1. 项目概述

本项目旨在开发一个基于静态网页技术的族谱系统，参考 Webtrees 功能，但采用完全静态的方式实现，可通过 GitHub Pages、Vercel、Netlify 等免费平台托管。

### 1.1 项目目标
- 提供家谱数据的静态展示功能
- 支持家族树可视化
- 实现数据导入/导出功能
- 保持开源、免费特性
- 方便代码管理和版本控制

### 1.2 技术选型

| 类别 | 技术/工具 | 版本 | 用途 |
|------|-----------|------|------|
| 前端框架 | React | 18+ | 构建用户界面 |
| 可视化库 | D3.js | 7+ | 家族树可视化 |
| 数据格式 | JSON | - | 存储家谱数据 |
| 构建工具 | Vite | 4+ | 项目构建和开发 |
| 托管平台 | GitHub Pages | - | 免费静态托管 |

## 2. 系统架构

### 2.1 架构图

```
┌─────────────────┐
│ 静态网页文件     │
├─────────────────┤
│ 前端应用 (React) │
├─────────────────┤
│ 可视化组件 (D3.js)│
├─────────────────┤
│ 数据层 (JSON)    │
└─────────────────┘
```

### 2.2 数据流
1. GEDCOM 文件 → JSON 转换
2. JSON 数据 → 前端组件渲染
3. 前端交互 → 可视化展示

## 3. 项目结构

```
family-tree/
├── public/
│   ├── data/             # 家谱数据 JSON 文件
│   │   └── family.json
│   └── index.html
├── src/
│   ├── components/       # 组件
│   │   ├── FamilyTree/   # 家族树组件
│   │   ├── PersonCard/   # 人物卡片组件
│   │   └── Navigation/   # 导航组件
│   ├── utils/            # 工具函数
│   │   ├── gedcomParser.js  # GEDCOM 解析器
│   │   └── treeBuilder.js   # 树结构构建器
│   ├── App.jsx           # 主应用
│   └── main.jsx          # 入口文件
├── package.json          # 项目配置
├── vite.config.js        # Vite 配置
└── README.md             # 项目说明
```

## 4. 核心功能实现

### 4.1 数据结构设计

#### 4.1.1 人物数据结构
```json
{
  "id": "I1",
  "name": "张三",
  "birth": "1900-01-01",
  "death": "1980-01-01",
  "gender": "M",
  "parents": ["I2", "I3"],
  "spouses": ["I4"],
  "children": ["I5", "I6"],
  "events": [
    {
      "type": "BIRT",
      "date": "1900-01-01",
      "place": "北京"
    }
  ]
}
```

#### 4.1.2 家族树数据结构
```json
{
  "individuals": {
    "I1": {...},
    "I2": {...}
  },
  "families": {
    "F1": {
      "husband": "I1",
      "wife": "I2",
      "children": ["I3", "I4"]
    }
  },
  "rootId": "I1"
}
```

### 4.2 家族树可视化

使用 D3.js 实现交互式家族树，支持点击头像查看个人详情：

```javascript
// src/components/FamilyTree/index.jsx
import React, { useEffect, useRef, useState } from 'react';
import * as d3 from 'd3';
import PersonCard from '../PersonCard';

const FamilyTree = ({ data, rootId }) => {
  const svgRef = useRef(null);
  const [selectedPerson, setSelectedPerson] = useState(null);

  useEffect(() => {
    if (!data || !rootId) return;

    const svg = d3.select(svgRef.current);
    svg.selectAll('*').remove();

    // 构建树状结构
    const treeLayout = d3.tree().size([800, 600]);
    const hierarchy = d3.hierarchy(buildTreeData(data, rootId));
    const treeData = treeLayout(hierarchy);

    // 绘制连接线
    svg.append('g')
      .selectAll('path')
      .data(treeData.links())
      .enter()
      .append('path')
      .attr('d', d3.linkHorizontal()
        .x(d => d.y)
        .y(d => d.x))
      .attr('stroke', '#ccc')
      .attr('fill', 'none');

    // 绘制节点
    const nodes = svg.append('g')
      .selectAll('g')
      .data(treeData.descendants())
      .enter()
      .append('g')
      .attr('transform', d => `translate(${d.y},${d.x})`)
      .on('click', (event, d) => {
        // 点击节点时，获取对应人物数据
        const personId = d.data.id;
        const person = data.individuals[personId];
        setSelectedPerson(person);
      });

    // 绘制头像/节点
    nodes.append('circle')
      .attr('r', 20)
      .attr('fill', d => {
        // 如果有头像，使用头像，否则使用默认颜色
        if (d.data.media && d.data.media.avatar) {
          return 'url(#' + d.data.id + '-avatar)';
        }
        return d.data.gender === 'M' ? '#4285F4' : '#EA4335';
      })
      .style('cursor', 'pointer')
      .style('stroke', '#fff')
      .style('stroke-width', 2);

    // 添加头像定义
    const defs = svg.append('defs');
    Object.values(data.individuals).forEach(person => {
      if (person.media && person.media.avatar) {
        defs.append('pattern')
          .attr('id', person.id + '-avatar')
          .attr('patternUnits', 'userSpaceOnUse')
          .attr('width', 40)
          .attr('height', 40)
          .append('image')
          .attr('x', 0)
          .attr('y', 0)
          .attr('width', 40)
          .attr('height', 40)
          .attr('href', `/media/${person.media.avatar}`);
      }
    });

    // 绘制姓名
    nodes.append('text')
      .attr('text-anchor', 'middle')
      .attr('dy', '.35em')
      .text(d => d.data.name)
      .attr('fill', 'white')
      .style('font-size', '12px');

  }, [data, rootId]);

  return (
    <div className="family-tree-container">
      <svg ref={svgRef} width="1000" height="800"></svg>
      {selectedPerson && (
        <PersonCard 
          person={selectedPerson} 
          onClose={() => setSelectedPerson(null)} 
        />
      )}
    </div>
  );
};

export default FamilyTree;
```

### 4.3 个人详情卡片组件

```javascript
// src/components/PersonCard/index.jsx
import React from 'react';
import './PersonCard.css';

const PersonCard = ({ person, onClose }) => {
  if (!person) return null;

  return (
    <div className="person-card-overlay" onClick={onClose}>
      <div className="person-card" onClick={(e) => e.stopPropagation()}>
        <button className="close-button" onClick={onClose}>×</button>
        
        <div className="person-header">
          <div className="avatar-container">
            {person.media && person.media.avatar ? (
              <img 
                src={`/media/${person.media.avatar}`} 
                alt={person.name} 
                className="avatar"
              />
            ) : (
              <div className="avatar-placeholder" style={{ 
                backgroundColor: person.gender === 'M' ? '#4285F4' : '#EA4335' 
              }}>
                {person.name.charAt(0)}
              </div>
            )}
          </div>
          <div className="person-info">
            <h2 className="person-name">{person.name}</h2>
            {person.alias && person.alias.length > 0 && (
              <p className="person-alias">{person.alias.join('，')}</p>
            )}
            {person.generationName && (
              <p className="person-generation">字辈：{person.generationName}</p>
            )}
          </div>
        </div>

        <div className="person-details">
          <div className="detail-section">
            <h3>基本信息</h3>
            <div className="detail-row">
              <span className="label">性别：</span>
              <span>{person.gender === 'M' ? '男' : person.gender === 'F' ? '女' : '未知'}</span>
            </div>
            {person.birth && (
              <div className="detail-row">
                <span className="label">出生日期：</span>
                <span>{person.birth.date} {person.birth.place && `(${person.birth.place})`}</span>
              </div>
            )}
            {person.death && (
              <div className="detail-row">
                <span className="label">逝世日期：</span>
                <span>{person.death.date} {person.death.place && `(${person.death.place})`}</span>
              </div>
            )}
            {person.origin && (
              <div className="detail-row">
                <span className="label">籍贯：</span>
                <span>{person.origin}</span>
              </div>
            )}
            {person.occupation && (
              <div className="detail-row">
                <span className="label">职业：</span>
                <span>{person.occupation}</span>
              </div>
            )}
            {person.education && (
              <div className="detail-row">
                <span className="label">教育背景：</span>
                <span>{person.education}</span>
              </div>
            )}
          </div>

          {person.achievements && person.achievements.length > 0 && (
            <div className="detail-section">
              <h3>成就与荣誉</h3>
              <ul className="achievements-list">
                {person.achievements.map((achievement, index) => (
                  <li key={index}>{achievement}</li>
                ))}
              </ul>
            </div>
          )}

          {person.biography && (
            <div className="detail-section">
              <h3>生平简介</h3>
              <p className="biography">{person.biography}</p>
            </div>
          )}

          {person.events && person.events.length > 0 && (
            <div className="detail-section">
              <h3>重要事件</h3>
              <div className="events-list">
                {person.events.map((event, index) => (
                  <div key={index} className="event-item">
                    <div className="event-type">
                      {event.type === 'BIRT' && '出生'}
                      {event.type === 'MARR' && '结婚'}
                      {event.type === 'DEAT' && '逝世'}
                      {event.type === 'BURI' && '埋葬'}
                      {event.type === 'OCCU' && '职业'}
                      {event.type === 'EDUC' && '教育'}
                      {!['BIRT', 'MARR', 'DEAT', 'BURI', 'OCCU', 'EDUC'].includes(event.type) && event.type}
                    </div>
                    <div className="event-details">
                      {event.date && <span className="event-date">{event.date}</span>}
                      {event.place && <span className="event-place">{event.place}</span>}
                      {event.notes && <span className="event-notes">{event.notes}</span>}
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}

          {(person.parents && person.parents.length > 0) && (
            <div className="detail-section">
              <h3>父母</h3>
              <div className="family-members">
                {person.parents.map(parentId => {
                  const parent = window.familyData?.individuals[parentId];
                  return parent ? (
                    <div key={parentId} className="family-member">
                      {parent.name}
                    </div>
                  ) : null;
                })}
              </div>
            </div>
          )}

          {(person.spouses && person.spouses.length > 0) && (
            <div className="detail-section">
              <h3>配偶</h3>
              <div className="family-members">
                {person.spouses.map(spouseId => {
                  const spouse = window.familyData?.individuals[spouseId];
                  return spouse ? (
                    <div key={spouseId} className="family-member">
                      {spouse.name}
                    </div>
                  ) : null;
                })}
              </div>
            </div>
          )}

          {(person.children && person.children.length > 0) && (
            <div className="detail-section">
              <h3>子女</h3>
              <div className="family-members">
                {person.children.map(childId => {
                  const child = window.familyData?.individuals[childId];
                  return child ? (
                    <div key={childId} className="family-member">
                      {child.name}
                    </div>
                  ) : null;
                })}
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  );
};

export default PersonCard;
```

### 4.4 个人详情卡片样式

```css
/* src/components/PersonCard/PersonCard.css */
.person-card-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.7);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.person-card {
  background-color: white;
  border-radius: 10px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
  width: 90%;
  max-width: 600px;
  max-height: 80vh;
  overflow-y: auto;
  position: relative;
  padding: 20px;
}

.close-button {
  position: absolute;
  top: 10px;
  right: 10px;
  background: none;
  border: none;
  font-size: 24px;
  cursor: pointer;
  color: #666;
}

.person-header {
  display: flex;
  align-items: center;
  margin-bottom: 20px;
  padding-bottom: 20px;
  border-bottom: 1px solid #eee;
}

.avatar-container {
  margin-right: 20px;
}

.avatar {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  object-fit: cover;
  border: 3px solid #fff;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
}

.avatar-placeholder {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  color: white;
  font-size: 36px;
  font-weight: bold;
  border: 3px solid #fff;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
}

.person-info h2 {
  margin: 0 0 5px 0;
  font-size: 24px;
  color: #333;
}

.person-alias {
  margin: 0 0 5px 0;
  color: #666;
  font-style: italic;
}

.person-generation {
  margin: 0;
  color: #888;
  font-size: 14px;
}

.person-details {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.detail-section h3 {
  margin: 0 0 10px 0;
  font-size: 18px;
  color: #333;
  border-bottom: 1px solid #eee;
  padding-bottom: 5px;
}

.detail-row {
  display: flex;
  margin-bottom: 8px;
}

.detail-row .label {
  font-weight: bold;
  width: 100px;
  color: #666;
}

.achievements-list {
  margin: 0;
  padding-left: 20px;
}

.achievements-list li {
  margin-bottom: 5px;
}

.biography {
  margin: 0;
  line-height: 1.6;
  color: #333;
}

.events-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.event-item {
  border-left: 3px solid #4285F4;
  padding-left: 10px;
}

.event-type {
  font-weight: bold;
  color: #4285F4;
}

.event-details {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 5px;
  font-size: 14px;
  color: #666;
}

.family-members {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.family-member {
  background-color: #f5f5f5;
  padding: 5px 10px;
  border-radius: 15px;
  font-size: 14px;
}

@media (max-width: 768px) {
  .person-card {
    width: 95%;
    max-height: 90vh;
  }
  
  .person-header {
    flex-direction: column;
    text-align: center;
  }
  
  .avatar-container {
    margin-right: 0;
    margin-bottom: 15px;
  }
  
  .detail-row {
    flex-direction: column;
  }
  
  .detail-row .label {
    width: 100%;
    margin-bottom: 5px;
  }
}
```

### 4.5 GEDCOM 转换工具

实现 GEDCOM 文件到 JSON 的转换：

```javascript
// src/utils/gedcomParser.js
const parseGedcom = (gedcomContent) => {
  const lines = gedcomContent.split('\n');
  const individuals = {};
  const families = {};
  let currentIndividual = null;
  let currentFamily = null;

  lines.forEach(line => {
    const match = line.match(/^(\d+) (\w+)(?: (.*))?$/);
    if (!match) return;

    const [, level, tag, value] = match;

    if (tag === 'INDI') {
      currentIndividual = value;
      individuals[currentIndividual] = {
        id: currentIndividual,
        name: '',
        birth: '',
        death: '',
        gender: '',
        parents: [],
        spouses: [],
        children: [],
        events: []
      };
    } else if (tag === 'FAM') {
      currentFamily = value;
      families[currentFamily] = {
        id: currentFamily,
        husband: '',
        wife: '',
        children: []
      };
    } else if (currentIndividual) {
      // 处理个人信息
      switch (tag) {
        case 'NAME':
          individuals[currentIndividual].name = value;
          break;
        case 'SEX':
          individuals[currentIndividual].gender = value;
          break;
        case 'BIRT':
          // 处理出生日期
          break;
        case 'DEAT':
          // 处理死亡日期
          break;
        case 'FAMC':
          // 处理父母家庭
          break;
        case 'FAMS':
          // 处理配偶家庭
          break;
      }
    } else if (currentFamily) {
      // 处理家庭信息
      switch (tag) {
        case 'HUSB':
          families[currentFamily].husband = value;
          break;
        case 'WIFE':
          families[currentFamily].wife = value;
          break;
        case 'CHIL':
          families[currentFamily].children.push(value);
          break;
      }
    }
  });

  return { individuals, families, rootId: Object.keys(individuals)[0] };
};

export default parseGedcom;
```

## 5. 部署方案

### 5.1 GitHub Pages 部署

1. **创建 GitHub 仓库**
   - 登录 GitHub，创建新仓库
   - 克隆仓库到本地

2. **构建项目**
   ```bash
   npm run build
   ```

3. **部署到 GitHub Pages**
   - 安装 `gh-pages` 包
   ```bash
   npm install gh-pages --save-dev
   ```
   
   - 在 `package.json` 中添加脚本
   ```json
   "scripts": {
     "deploy": "gh-pages -d dist"
   }
   ```
   
   - 执行部署
   ```bash
   npm run deploy
   ```

4. **配置 GitHub Pages**
   - 进入仓库设置 → Pages
   - 选择 `gh-pages` 分支作为发布源
   - 保存设置，等待部署完成

### 5.4 Gitee Pages 部署

1. **创建 Gitee 仓库**
   - 登录 Gitee，创建新仓库
   - 克隆仓库到本地
   ```bash
   git clone https://gitee.com/your-username/family-tree.git
   cd family-tree
   ```

2. **构建项目**
   ```bash
   npm run build
   ```

3. **部署到 Gitee Pages**
   - 推送构建产物到仓库
   ```bash
   git add .
   git commit -m "Build and deploy"
   git push origin main
   ```

4. **配置 Gitee Pages**
   - 进入仓库设置 → Pages
   - 选择分支：`main`
   - 选择目录：`dist`
   - 点击 "启动"
   - 等待部署完成，获取访问 URL

5. **媒体文件处理**
   - 将头像等媒体文件放置在 `public/media/` 目录
   - 构建时会自动复制到 `dist/media/` 目录
   - 确保在数据结构中正确引用媒体文件路径

6. **免费托管说明**
   - Gitee Pages 提供免费的静态网站托管服务
   - 支持自定义域名（需在仓库设置中配置）
   - 无流量限制，适合中小型族谱网站
   - 媒体文件大小限制：单个文件不超过 100MB，总存储不超过 1GB

### 5.2 Vercel 部署

1. **连接 GitHub 仓库**
   - 登录 Vercel
   - 点击 "New Project"
   - 选择你的 GitHub 仓库

2. **配置部署**
   - 选择框架：React
   - 构建命令：`npm run build`
   - 输出目录：`dist`

3. **部署**
   - 点击 "Deploy"
   - 等待部署完成
   - 获取部署 URL

### 5.3 Netlify 部署

1. **连接 GitHub 仓库**
   - 登录 Netlify
   - 点击 "Add new site" → "Import an existing project"
   - 选择你的 GitHub 仓库

2. **配置部署**
   - 构建命令：`npm run build`
   - 发布目录：`dist`

3. **部署**
   - 点击 "Deploy site"
   - 等待部署完成
   - 获取部署 URL

## 6. 数据管理

### 6.1 数据导入

1. **GEDCOM 文件导入**
   - 使用 `gedcomParser.js` 工具转换 GEDCOM 文件
   - 生成 JSON 数据文件
   - 放置到 `public/data/` 目录

2. **手动编辑数据**
   - 直接编辑 `family.json` 文件
   - 按照 JSON 结构添加或修改数据

### 6.2 数据导出

1. **导出为 JSON**
   - 直接下载 `family.json` 文件

2. **导出为 GEDCOM**
   - 实现 JSON 到 GEDCOM 的转换工具
   - 生成 GEDCOM 文件

## 7. 性能优化

### 7.1 数据处理优化
- 分层加载：大数据量时采用分页或按需加载
- 数据压缩：使用 JSON 压缩减少文件大小
- 缓存策略：利用浏览器缓存静态资源

### 7.2 渲染优化
- 虚拟滚动：处理大量节点时使用虚拟滚动
- 懒加载：非可视区域的节点延迟渲染
- Web Worker：将数据处理移至后台线程

## 8. 扩展功能

### 8.1 可选功能
- **用户认证**：使用 Netlify Identity 或 Firebase Auth
- **评论系统**：集成 Disqus 或 Utterances
- **搜索功能**：实现客户端搜索
- **主题切换**：支持明暗主题
- **表单提交添加新人**：使用 GitHub/Gitee API 或 Serverless Functions

### 8.2 未来扩展
- **Serverless API**：添加动态功能
- **数据库集成**：使用 Supabase 或 Firebase
- **多语言支持**：添加国际化

## 9. 表单提交添加新人功能

### 9.1 纯静态方案：使用 GitHub/Gitee API

**实现原理**：
- 创建一个表单页面，收集新成员信息
- 使用 GitHub/Gitee API 将数据提交到仓库
- 管理员审核后合并更改
- 网站自动重新构建部署

**具体步骤**：
1. **创建表单页面**：设计一个包含所有必要字段的表单
2. **实现表单提交**：使用 JavaScript 调用 GitHub/Gitee API
3. **配置 API 权限**：创建一个具有写入权限的令牌
4. **设置审核流程**：管理员收到通知后审核更改
5. **自动部署**：审核通过后，网站自动重新构建部署

**优点**：
- 完全静态，无需数据库
- 利用版本控制，可追踪所有更改
- 适合小团队或个人使用

**缺点**：
- 需要配置 API 权限
- 审核流程需要手动操作
- 不适合频繁添加成员的场景

### 9.2 半静态方案：使用 Serverless Functions

**实现原理**：
- 前端使用静态表单
- 后端使用 Serverless Functions 处理表单提交
- 数据存储在静态 JSON 文件中
- 管理员审核后更新数据

**具体步骤**：
1. **创建表单页面**：设计表单收集新成员信息
2. **部署 Serverless Function**：使用 Netlify/Vercel Functions
3. **实现数据处理**：Function 接收表单数据，生成新的 JSON
4. **设置审核机制**：管理员收到通知，审核通过后更新数据
5. **重新部署**：数据更新后，网站自动重新部署

**优点**：
- 无需数据库
- 表单提交体验更好
- 可实现更复杂的验证逻辑

**缺点**：
- 需要配置 Serverless Functions
- 依赖第三方服务（Netlify/Vercel）
- 有一定的学习成本

### 9.3 实现示例

**表单页面代码**：
```html
<form id="addPersonForm">
  <input type="text" name="name" placeholder="姓名" required>
  <input type="date" name="birth" placeholder="出生日期">
  <select name="gender">
    <option value="M">男</option>
    <option value="F">女</option>
  </select>
  <button type="submit">提交</button>
</form>

<script>
  document.getElementById('addPersonForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(e.target);
    const personData = Object.fromEntries(formData);
    
    // 调用 GitHub/Gitee API
    const response = await fetch('https://api.github.com/repos/your-username/family-tree/contents/public/data/family.json', {
      method: 'PUT',
      headers: {
        'Authorization': `token ${YOUR_TOKEN}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        message: 'Add new family member',
        content: btoa(JSON.stringify({
          ...existingData,
          individuals: {
            ...existingData.individuals,
            [`I${Date.now()}`]: {
              id: `I${Date.now()}`,
              ...personData,
              parents: [],
              spouses: [],
              children: [],
              events: []
            }
          }
        })),
        sha: existingFileSha
      })
    });
    
    if (response.ok) {
      alert('提交成功，等待管理员审核');
    } else {
      alert('提交失败，请重试');
    }
  });
</script>
```

### 9.4 管理员审核流程

1. **收到通知**：GitHub/Gitee 会发送通知给仓库管理员
2. **审核更改**：管理员查看提交的新成员信息
3. **合并更改**：审核通过后，合并 Pull Request
4. **自动部署**：合并后，网站自动重新构建部署
5. **验证结果**：管理员验证新成员是否正确显示在家族树中

### 9.5 数据安全考虑

- **API 令牌保护**：不要在前端代码中直接暴露 API 令牌
- **数据验证**：在提交前进行客户端验证
- **权限控制**：确保只有授权人员可以提交更改
- **审核机制**：所有更改都经过管理员审核，防止恶意数据

### 9.6 最佳实践

- **使用分支**：提交到单独的分支，便于审核
- **详细提交信息**：提供清晰的提交信息，说明添加的成员信息
- **数据备份**：定期备份 JSON 数据文件
- **错误处理**：添加适当的错误处理，提高用户体验
- **测试**：在部署前测试表单提交功能

## 10. 开发流程

### 10.1 开发环境设置

1. **初始化项目**
   ```bash
   npm create vite@latest family-tree -- --template react
   cd family-tree
   npm install
   ```

2. **安装依赖**
   ```bash
   npm install d3
   ```

3. **启动开发服务器**
   ```bash
   npm run dev
   ```

### 10.2 代码管理

1. **版本控制**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **分支管理**
   - `main`：主分支，用于生产部署
   - `develop`：开发分支，用于功能开发
   - `feature/*`：特性分支，用于具体功能开发

3. **代码规范**
   - 使用 ESLint 和 Prettier
   - 遵循 Airbnb 代码规范

## 11. 维护与支持

### 11.1 问题排查
- 检查浏览器控制台错误
- 验证 JSON 数据格式
- 确认部署配置正确

### 11.2 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 家族树不显示 | JSON 数据格式错误 | 检查数据结构，确保格式正确 |
| 页面加载缓慢 | 数据量过大 | 实现分层加载或数据分页 |
| 部署失败 | 构建配置错误 | 检查构建命令和输出目录 |

### 11.3 贡献指南
- Fork 仓库
- 创建特性分支
- 提交更改
- 发起 Pull Request

## 12. 许可证

本项目采用 MIT 许可证，详见 LICENSE 文件。

## 13. 参考资源

- [Webtrees 官方网站](https://webtrees.net/)
- [D3.js 文档](https://d3js.org/)
- [React 文档](https://react.dev/)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Vercel 文档](https://vercel.com/docs)
- [Netlify 文档](https://docs.netlify.com/)

## 14. 项目启动清单

- [ ] 初始化 React 项目
- [ ] 安装必要依赖
- [ ] 创建基本项目结构
- [ ] 实现数据结构
- [ ] 开发家族树组件
- [ ] 实现 GEDCOM 转换工具
- [ ] 添加示例数据
- [ ] 测试本地构建
- [ ] 部署到托管平台
- [ ] 验证部署结果
- [ ] 编写 README 文档

## 15. 开发里程碑

| 里程碑 | 完成时间 | 主要任务 | 交付物 |
|--------|----------|----------|--------|
| 项目初始化 | 第1天 | 初始化 React 项目、安装依赖、配置开发环境 | 项目基础结构 |
| 数据层实现 | 第3天 | 设计数据结构、实现 GEDCOM 转换工具、创建示例数据 | 数据处理模块 |
| 前端核心功能 | 第6天 | 开发家族树可视化组件、实现人物卡片、添加导航功能 | 核心前端组件 |
| 密码保护功能 | 第7天 | 实现客户端密码验证、优化用户体验 | 安全访问机制 |
| 测试与优化 | 第8天 | 功能测试、性能优化、兼容性测试 | 测试报告 |
| 部署与上线 | 第9天 | 配置部署流程、部署到 Gitee Pages、验证访问 | 线上网站 |
| 文档完善 | 第10天 | 编写 README 文档、使用说明、维护指南 | 完整项目文档 |

## 16. 开发时间安排

### 详细时间计划

| 日期 | 工作内容 | 负责人 | 完成标准 |
|------|----------|--------|----------|
| 第1天 | 项目初始化与环境搭建 | 开发人员 | 成功创建 React 项目，安装必要依赖，启动开发服务器 |
| 第2天 | 数据结构设计与实现 | 开发人员 | 完成人物和家族树数据结构设计，实现基本数据模型 |
| 第3天 | GEDCOM 转换工具开发 | 开发人员 | 实现 GEDCOM 文件解析，转换为 JSON 格式 |
| 第4-5天 | 家族树可视化组件开发 | 开发人员 | 使用 D3.js 实现交互式家族树，支持缩放、平移、点击查看 |
| 第6天 | 人物卡片与导航功能 | 开发人员 | 实现人物详情卡片，添加导航菜单，优化用户界面 |
| 第7天 | 密码保护功能实现 | 开发人员 | 添加客户端密码验证，确保网站安全访问 |
| 第8天 | 测试与性能优化 | 开发人员 | 功能测试，性能优化，兼容性测试，修复问题 |
| 第9天 | 部署到 Gitee Pages | 开发人员 | 配置部署流程，成功部署到 Gitee Pages，验证访问 |
| 第10天 | 文档编写与项目总结 | 开发人员 | 编写完整的 README 文档，使用说明，维护指南 |

### 资源需求

| 资源 | 需求 | 备注 |
|------|------|------|
| 开发环境 | Node.js 16+，npm 8+ | 用于项目构建和依赖管理 |
| 开发工具 | VS Code，Git | 代码编辑和版本控制 |
| 托管平台 | Gitee Pages | 免费静态网站托管 |
| 可视化库 | D3.js | 用于家族树可视化 |
| 前端框架 | React | 构建用户界面 |

### 风险与应对措施

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 数据量过大导致性能问题 | 中 | 页面加载缓慢，交互卡顿 | 实现分层加载，数据分页，虚拟滚动 |
| GEDCOM 解析复杂 | 中 | 解析失败，数据丢失 | 完善错误处理，添加数据验证 |
| 部署配置错误 | 低 | 网站无法访问 | 详细记录部署步骤，测试部署流程 |
| 密码保护安全性 | 低 | 密码可能被破解 | 使用加密存储，定期更新密码 |

---

**项目状态：** 开发中
**最后更新：** 2026-04-18
**作者：** 族谱项目团队